# Production environment - use nginx reverse proxy
# All API calls go through nginx on port 80

VITE_USE_PROXY=true

# These are not needed when using proxy, but kept for reference
# VITE_INTEGRATION_SERVICE_URL=/api/integration
# VITE_DESIGN_SERVICE_URL=/api/design
# VITE_CAPABILITY_SERVICE_URL=/api/capability
# VITE_AUTH_SERVICE_URL=/api/auth
